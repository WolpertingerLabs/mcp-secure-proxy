# MCP Secure Proxy — Remote Server Secrets
#
# Copy this file to .env and fill in your actual secret values.
# This file is loaded by docker-compose and passed as environment variables
# into the container. The values are never baked into the Docker image.
#
# In config.json, reference these as:
#   "secrets": {
#     "OPENAI_API_KEY": "${OPENAI_API_KEY}",
#     "GITHUB_TOKEN": "${GITHUB_TOKEN}"
#   }

OPENAI_API_KEY=
GITHUB_TOKEN=
